---
name: component-quote-backto
description: Develop Quote and BackTo components in @ant-design/agentic-ui. Use when building quote block or back navigation link. Triggers on Quote, BackTo, quote block, back link, navigation.
---

# Quote / BackTo 组件开发

引用块（Quote）与返回链接（BackTo）的展示与交互。

## 源码位置

- Quote：`src/Quote/`（默认导出、QuoteProps）
- BackTo：`src/BackTo/`

## 开发规范

- 使用 token 与 createStyles；与 MarkdownEditor 的 blockquote、ChatLayout 导航等配合时保持风格一致。

修改 Quote 或 BackTo 时，参考对应 `src/` 目录与 `AGENTS.md`。
